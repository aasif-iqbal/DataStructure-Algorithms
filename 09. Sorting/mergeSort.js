//merge sort - O(n log(n)) at best/avg/worst and uses Divided & conquer technique

const numbers = [2,4,1,6,8,5,3,7];

function mergeSort(array){
    if(array.length === 1){
        return array;
    }

    //split array into left and right
    
    return merge(mergeSort(left),)
}

const result = mergeSort(numbers);
console.log(result);
